# Task1 repository
README.md - project descrtiption file
src/Main.java - java print program

git remote add origin <URL> - connect the local repository to the remote one
git remote -v - verify whether the origin addresses were
added correctly
git push -u origin main - send the history and set the upstream for the
main branch
